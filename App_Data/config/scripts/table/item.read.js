function read(query, user, request) {
  /*query.where((user)=>{//{ userId: user.userId });
        return this.userId == user.userId
            || this.users.indexOf(user.userId) > -1;
   }, user);*/
   request.execute();
}